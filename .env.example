# Configure these variables in the Netlify project before deploying.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CONTACT_EMAIL_TO=
# Required for Netlify builds; use the final public HTTPS origin without a trailing slash.
NEXT_PUBLIC_SITE_URL=
